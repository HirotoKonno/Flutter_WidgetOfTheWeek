name: flutter_widget_of_the_week
description: For learn flutter.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.18
  location: ^5.0.3
  device_info: ^2.0.3
  cupertino_icons: ^1.0.2
  flutter_slidable: ^3.0.0
  connectivity: ^3.0.6
  animated_text_kit: ^4.2.2
  sensors_plus: ^1.3.2
#  flutter_html: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - images/loading.jpg